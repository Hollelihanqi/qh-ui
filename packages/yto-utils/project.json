{
  "name": "yto-utils",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/yto-utils/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "inputs": ["{projectRoot}/**/*", "!{projectRoot}/dist/**/*"],
      "options": {
        "command": "cd packages/yto-utils && pnpm build",
        "cwd": "."
      }
    }
  }
}
